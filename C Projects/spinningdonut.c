#include <stdio.h> 
#include <string.h> 
#include <stdlib.h> 
#include <time.h> 
#include <math.h> 
#include <windows.h>

void main() {
    float A = 0, B = 0; float i, j;
    int k;
    float z[1760]; char b[1760]; printf("\x1b[2J");
    while(1<2) {
        memset(b, 32, 1760);
        memset(z, 0, 7040);
        for (j = 0; j < 6.28; j += 0.07) {
            float c = sin(i);
            float d = cos(j);
            float e = sin(A);
            float f = sin(j);
            float g = cos(A);
            float h = d + 2;
            float D = 1 / (c * h * e + f float l = cos(i);
{
* g + 5);
* m - t * * n + t *
float m = cos(B); float n = sin(B); float t = c * h * int x = 40 + 30 * int y = 12 + 15 * int o = x + 80 * y; int N = 8 * ((f * e if (22 > y && y > 0 z[o] = D;
b[o] = ".,-~:;=!*#$@"[N > 0 ? N : 0];